export const sampleProjectDump = `Nokta Distillery should turn scattered project chats, notes, and fragments into maintained project memory.
Maybe it only needs to summarize the notes.
No, not only summarize; the output has to become a reviewable wiki patch preview.
Users must confirm facts before anything becomes trusted memory.
Human review states should include pending, confirmed, needs review, discarded, and locked.
We need one canonical project summary first.
Role tabs should come from that canonical summary: Product, Designer, Frontend, Backend, AI/LLM.
Role-specific views should all be projected from one canonical summary rather than separate summaries.
Source traceability matters. Evidence ledger matters. Supporting sources should stay visible.
Evidence ledger should map important claims back to source fragments so the memory bundle stays auditable.
The app should surface contradictions instead of smoothing them over.
Question generation should be targeted and useful, not a flood of generic prompts.
No live AI API for MVP. Use a local deterministic engine.
The first version probably does not need a real backend.
But the UI should still look backend-driven through a structured response shape.
The app must not look like a chatbot.
The app must feel like a project-memory distillation cockpit.
Wiki Patch Preview is the signature feature.
Wiki patch output should show which files would be created or appended and why.
Handoff pack should help the next model or collaborator continue the project.
Maybe the app should save automatically without asking.
No, automatic trust would break the product. Human review must stay in the loop.
Ambiguity budget should show what is still unclear.
We need a visible processing pipeline, not just a spinner.
Messy fragments should become source fragments, idea cards, canonical memory, role views, evidence, wiki patch, and handoff pack.
Future feature: real markdown persistence can come later.
Non-goal: this should not collapse into a generic note app or generic summarizer.`;
